#include <stdio.h>

int main(){
    
    // arithemtic operations

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (nodulus)
    // ++ increment
    // -- decrement

    int x = 5;
    int y = 2;

    int z = x + y;

    printf("%d\n", z);

    return 0;
}